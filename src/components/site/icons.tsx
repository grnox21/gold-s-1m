import type { SVGProps } from "react";

/** lucide-react dropped brand/logo glyphs (Instagram included) — this is a
 * minimal outline replacement drawn in the same stroke style (24x24,
 * round caps/joins) so it sits seamlessly next to the rest of the icon set. */
export function InstagramIcon(props: SVGProps<SVGSVGElement>) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.5}
      strokeLinecap="round"
      strokeLinejoin="round"
      {...props}
    >
      <rect x="3" y="3" width="18" height="18" rx="5" />
      <circle cx="12" cy="12" r="4.2" />
      <circle cx="17.2" cy="6.8" r="0.6" fill="currentColor" stroke="none" />
    </svg>
  );
}
